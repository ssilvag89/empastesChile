---
import { Image } from 'astro:assets';
import myImage1 from '/public/assets/carousel/carr_1.png'; 
import myImage2 from '/public/assets/carousel/carr_2.png'; 
import myImage3 from '/public/assets/carousel/carr_3.png'; 
---

<!-- SECCIÓN PRINCIPAL QUE CONTIENE TODAS LAS SECCIONES -->
<script>
  document.addEventListener("DOMContentLoaded", function () {
  const elements = document.querySelectorAll("h1, img");

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.style.animationPlayState = "running";
      }
    });
  });

  elements.forEach((el) => {
    observer.observe(el);
  });
});
</script>
<section id="hero" class="main-sections">
  <div class="parent">
    <div class="section-wrapper">
      <div class="section-content">
        <h1>EXCELENCIA EN ENCUADERNACIÓN Y EMPASTES</h1>
        <hr class="hr-hero">
        <h2>Con más de tres décadas de experiencia, transformamos libros y documentos en auténticas obras de arte.</h2>
      </div>
      <div class="section-image-container">
        <Image 
          src={myImage1} 
          alt="32 Años de Excelencia en Encuadernación y Empastes"
          loading="eager" 
          class="section-image"/>
      </div>
    </div>
    <div class="section-wrapper">
      <div class="section-image-container">
        <Image 
          src={myImage2} 
          alt="Compromiso y Calidad que Perduran en el Tiempo"
          loading="eager" 
          class="section-image"/>
      </div>
      <div class="section-content">
        <h1>COMPROMISO Y CALIDAD QUE PERDURAN EN EL TIEMPO</h1>
         <hr class="hr-hero">
        <h2>Nuestra pasión por la calidad garantiza resultados que superan expectativas, cuidando cada detalle para preservar tus libros y documentos con elegancia y distinción.</h2>
      </div>
    </div>

    <div class="section-wrapper">
      <div class="section-content">
        <h1>CALIDAD Y ELEGANCIA AL MEJOR PRECIO</h1>
         <hr class="hr-hero">
        <h2>Ofrecemos servicios de empaste y encuadernación con precios diseñados para ajustarse a tu presupuesto.</h2>
      </div>
      <div class="section-image-container">
        <Image 
          src={myImage3} 
          alt="Calidad y Elegancia al Mejor Precio"
          loading="lazy" 
          class="section-image"/>
      </div>
    </div>
</div>
</section> <!-- Cierre del main-sections -->
<style>
  .main-sections {
    width: 100%;
    padding-top: 8rem;
    display: flex;
    flex-direction: column;
    gap: 10rem;
  }

  .parent {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: auto;
    gap: 80px;
  }

  .section-wrapper {
    display: flex;
    align-items: center;
    gap: 40px;
    width: 100%;
    justify-content: space-between;
  }

  .section-content {
    flex: 1;
    color: #fff;
  }

  .section-content h1 {
    font-size: 2.2rem;
    margin-bottom: 10px;
    animation: fadeInUp 1s ease forwards;
  }

  .section-content h2 {
    font-size: 1.2rem;
    font-weight: 400;
    color: #ccc;
    line-height: 1.6;
  }
.hr-hero {
  width: 80%;
  height: 3px;
  background: linear-gradient(90deg, #4caf50, #d4af37, #ff0000, #4caf50);
  background-size: 400% 100%;
  animation: gradient-move 4s ease-in-out infinite, fadeInUp 1s ease forwards;
  border: none;
  margin-bottom: 20px;
  margin-top: 10px;
}

/* Efecto de animación del degradado que se mueve */
@keyframes gradient-move {
  0% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 50%;
  }
}

  .section-image-container {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .section-image {
    max-width: 100%;
    height: auto;
    animation: float 4s ease-in-out infinite;
  }

  @keyframes float {
    0%   { transform: translateY(0); }
    50%  { transform: translateY(-10px); }
    100% { transform: translateY(0); }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* 📱 Mobile view */
  @media (max-width: 768px) {
    .section-wrapper {
      flex-direction: column;
      gap: 30px;
      text-align: center;
      padding: 20px;
    }

    .section-content h1 {
      font-size: 1.5rem;
    }

    .section-content h2 {
      font-size: 1rem;
    }

    .hr-hero {
      width: 100%;
      margin: 10px auto 20px;
    }

    .section-image {
      width: 90%;
    }
  }

  /* Asegura que todo esté correctamente alineado en móvil */
@media (max-width: 768px) {
  .main-sections {
    padding: 4rem 1rem; /* Espaciado lateral para evitar que se vea hacia la derecha */
    padding-top: 120px; /* un poco menos en móvil si el logo es más pequeño */
    gap: 5rem;

  }

  .parent {
    gap: 60px;
  }

  .section-wrapper {
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 0;
    margin: 0 auto;
    width: 100%;
    max-width: 100%;
    box-sizing: border-box;
  }

  .section-content,
  .section-image-container {
    width: 100%;
    max-width: 100%;
    text-align: center;
  }

  .section-image {
    width: 100%;
    max-width: 90vw;
    height: auto;
  }

  .section-content h1,
  .section-content h2 {
    padding: 0 1rem;
  }
}
</style>

